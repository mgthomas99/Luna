
class "Point" {

	new = function(self, x, y)
		self.x = x
		self.y = y
	end,

	move = function(self, x, y)
		self.x = self.x + x
		self.y = self.y + y
	end,

	getX = function(self)
		return self.x
	end,

	getY = function(self)
		return self.y
	end

}
